pre-commit:
  parallel: true
  jobs:
    - name: elixir-format
      glob: "*.{ex,exs,heex}"
      run: mix format --check-formatted {staged_files}
    - name: elixir-test
      glob: "*.{ex,exs,heex}"
      run: mix test
